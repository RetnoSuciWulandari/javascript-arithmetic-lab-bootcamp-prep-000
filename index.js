var a, b;

function add(a, b) {
  return a + b;
}
add();

function subtract(a, b) {
  return a - b;
}
add();

function multiply(a, b) {
  return a * b;
}
add();

function divide(a, b) {
  return a / b;
}
add();

function inc(n) {
  return n+=1;
}
inc();

function dec(n) {
  return n-=1;
}
inc();

function makeInt(n) {
  return parseInt('n');
} 
makeInt('10');

function makeInt() {
  
}